/**
 * Interruptor centralizado de la sección Premium. Mientras esté en `false`, el
 * candado se mantiene en el sidebar y /premium solo muestra el placeholder
 * "Próximamente" — no hay lógica de pago ni restricciones reales todavía.
 * Cambiar a `true` cuando el sistema de pago esté listo para activarse.
 */
export const PREMIUM_ENABLED = false;
